# Google Service Account credentials
# See README.md for setup instructions

# Option 1 (recommended for Vercel): Base64-encode the entire JSON key file
# Run: base64 -i your-key-file.json (macOS/Linux) or use an online tool
GOOGLE_CREDENTIALS_BASE64=

# Option 2 (for local dev): Individual environment variables
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Required: Google Calendar IDs (one per location)
GOOGLE_CALENDAR_ID_BALI=your-bali-calendar-id@group.calendar.google.com
GOOGLE_CALENDAR_ID_SYDNEY=your-sydney-calendar-id@group.calendar.google.com

# Email configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="Julian Photography <your-email@gmail.com>"
JULIAN_EMAIL=julian@example.com
